apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-server
spec:
  ingressClassName: nginx
  rules:
    - host: 67ed3ae07e0e.ngrok.app
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: go-server
                port:
                  number: 80